interface I_Themes {
    textColor: string;
    bgColor: string;
    itemColor: string;
    acc_textColor: string;
    acc_itemColor: string;
};

export const LightTheme = {};

export const DarkTheme = {};